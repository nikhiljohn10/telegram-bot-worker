{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "build:worker": "lerna run build --scope=worker",
    "release": "lerna version minor -y",
    "publish": "lerna publish --scope=main"
  },
  "devDependencies": {
    "lerna": "^6.3.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
